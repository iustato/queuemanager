name: queue2

schema_file: queue1.schema.json

max_size: 1000
workers: 300

runtime: php-fpm
script: scripts/job_flaky.php

fpm_network: unix
fpm_address: /run/php/php8.3-fpm.sock

timeout_sec: 30
max_queue: 1000
max_retries: 3
retry_delay_ms: 2000

result_ttl: "40000m"
